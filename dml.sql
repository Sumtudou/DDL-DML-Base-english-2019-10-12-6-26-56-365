-- Insert record   id	name	age	sex
	insert into student (id,name,age,sex) values ("003","Tom",18,"male");
	
-- Revise record
	update student set age=28 where id="001" ;
	
--  Delete record
	delete from student where where id="001" ;

-- Search record
	select * from student where where id="001" ;